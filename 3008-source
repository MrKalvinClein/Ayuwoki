local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local VirtualUser = game:GetService("VirtualUser")

local isMobile = UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled

local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()

local PS = game:GetService("Players")
local WS = game:GetService("Workspace")
local Player = PS.LocalPlayer
local PlayerGui = Player:WaitForChild("PlayerGui")
local Character = PS.LocalPlayer.Character or PS.LocalPlayer.CharacterAdded:Wait()
local Backpack = Player:WaitForChild("Backpack")
local HumanoidRoot = Character:WaitForChild("HumanoidRootPart")
local ItemsFolder = WS:WaitForChild("GameObjects").Physical.Items
local Storage = PlayerGui:WaitForChild("MainGui").Menus.Inventory
local Pickup = Character.System.Action
local Camera = workspace.CurrentCamera

local ESPObjects = {}
local ESPSettings = {
    Players = false,
    Employees = false,
    Food = false
}

local ESPUpdateRate = 0.1

local function createESP(object, color, customName)
    local esp = {
        Object = object,
        Color = color,
        CustomName = customName,
        Drawings = {}
    }
    
    for i = 1, 4 do
        local line = Drawing.new("Line")
        line.Visible = false
        line.Color = color
        line.Thickness = 2
        line.Transparency = 1
        table.insert(esp.Drawings, line)
    end
    
    local tracer = Drawing.new("Line")
    tracer.Visible = false
    tracer.Color = color
    tracer.Thickness = 1
    tracer.Transparency = 1
    table.insert(esp.Drawings, tracer)
    
    local text = Drawing.new("Text")
    text.Visible = false
    text.Color = color
    text.Size = 14
    text.Center = true
    text.Outline = true
    text.Font = 2
    table.insert(esp.Drawings, text)
    
    return esp
end

local function removeESP(esp)
    for _, drawing in pairs(esp.Drawings) do
        drawing:Remove()
    end
end

local lastESPUpdate = 0
local function updateESP()
    local currentTime = tick()
    if currentTime - lastESPUpdate < ESPUpdateRate then
        return
    end
    lastESPUpdate = currentTime
    
    for _, esp in pairs(ESPObjects) do
        if esp.Object and esp.Object.Parent then
            local hrp = esp.Object:FindFirstChild("HumanoidRootPart") or esp.Object:FindFirstChild("Root") or esp.Object:FindFirstChildWhichIsA("Part") or esp.Object:FindFirstChildWhichIsA("MeshPart")
            
            if hrp then
                local vector, onScreen = Camera:WorldToViewportPoint(hrp.Position)
                local distance = (Camera.CFrame.Position - hrp.Position).Magnitude
                
                if onScreen and distance < 500 then
                    local size = (Camera:WorldToViewportPoint(hrp.Position - Vector3.new(0, 3, 0)).Y - Camera:WorldToViewportPoint(hrp.Position + Vector3.new(0, 2.6, 0)).Y) / 2
                    local boxSize = Vector2.new(math.floor(size * 1.5), math.floor(size * 2))
                    local boxPosition = Vector2.new(math.floor(vector.X - size * 1.5 / 2), math.floor(vector.Y - size * 2 / 2))
                    
                    esp.Drawings[1].From = boxPosition
                    esp.Drawings[1].To = boxPosition + Vector2.new(boxSize.X, 0)
                    esp.Drawings[1].Visible = true
                    
                    esp.Drawings[2].From = boxPosition + Vector2.new(boxSize.X, 0)
                    esp.Drawings[2].To = boxPosition + boxSize
                    esp.Drawings[2].Visible = true
                    
                    esp.Drawings[3].From = boxPosition + boxSize
                    esp.Drawings[3].To = boxPosition + Vector2.new(0, boxSize.Y)
                    esp.Drawings[3].Visible = true
                    
                    esp.Drawings[4].From = boxPosition + Vector2.new(0, boxSize.Y)
                    esp.Drawings[4].To = boxPosition
                    esp.Drawings[4].Visible = true
                    
                    esp.Drawings[5].From = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y)
                    esp.Drawings[5].To = Vector2.new(vector.X, vector.Y)
                    esp.Drawings[5].Visible = true
                    
                    local displayName = esp.CustomName or esp.Object.Name
                    esp.Drawings[6].Text = displayName .. " [" .. math.floor(distance) .. "m]"
                    esp.Drawings[6].Position = Vector2.new(vector.X, boxPosition.Y - 16)
                    esp.Drawings[6].Visible = true
                else
                    for _, drawing in pairs(esp.Drawings) do
                        drawing.Visible = false
                    end
                end
            else
                for _, drawing in pairs(esp.Drawings) do
                    drawing.Visible = false
                end
            end
        else
            for _, drawing in pairs(esp.Drawings) do
                drawing.Visible = false
            end
        end
    end
end

local function addESPToObject(object, color, customName)
    local esp = createESP(object, color, customName)
    table.insert(ESPObjects, esp)
    return esp
end

local function clearESP()
    for _, esp in pairs(ESPObjects) do
        removeESP(esp)
    end
    ESPObjects = {}
end

local function refreshESP()
    clearESP()
    
    if ESPSettings.Players then
        for _, player in pairs(PS:GetPlayers()) do
            if player ~= Player and player.Character then
                addESPToObject(player.Character, Color3.fromRGB(255, 165, 0), player.DisplayName)
            end
        end
    end
    
    if ESPSettings.Employees then
        for _, employee in pairs(WS.GameObjects.Physical.Employees:GetChildren()) do
            if employee:IsA("Model") then
                addESPToObject(employee, Color3.fromRGB(255, 0, 0), "Employee")
            end
        end
    end
    
    if ESPSettings.Food then
        local count = 0
        for _, item in pairs(ItemsFolder:GetChildren()) do
            if item:IsA("Model") and count < 50 then
                addESPToObject(item, Color3.fromRGB(0, 100, 255), item.Name)
                count = count + 1
            end
        end
    end
end

RunService.RenderStepped:Connect(updateESP)

PS.PlayerAdded:Connect(function(player)
    if ESPSettings.Players then
        player.CharacterAdded:Connect(function(char)
            task.wait(1)
            refreshESP()
        end)
    end
end)

PS.PlayerRemoving:Connect(function()
    if ESPSettings.Players then
        task.wait(1)
        refreshESP()
    end
end)

local function getUniqueItems()
    local uniqueItems = {}
    local itemNames = {}
    
    for i, v in pairs(ItemsFolder:GetChildren()) do
        if v:IsA("Model") and not table.find(itemNames, v.Name) then
            table.insert(itemNames, v.Name)
        end
    end
    
    table.sort(itemNames)
    return itemNames
end

local function teleportToItem(itemName)
    local found = false
    
    for i, v in pairs(ItemsFolder:GetChildren()) do
        if v:IsA("Model") and v.Name == itemName then
            local rootPart = v:FindFirstChild("Root") or v:FindFirstChild(itemName) or v:FindFirstChildWhichIsA("Part") or v:FindFirstChildWhichIsA("MeshPart")
            if rootPart then
                Player.Character.HumanoidRootPart.CFrame = rootPart.CFrame
                found = true
                WindUI:Notify({
                    Title = "Teleport",
                    Content = "Teleported to " .. itemName,
                    Duration = 3,
                })
                break
            end
        end
    end
    
    if not found then
        WindUI:Notify({
            Title = "Error",
            Content = itemName .. " not found in the map",
            Duration = 3,
        })
    end
end

local TimesToTeleport = 50
local Radius = 100

local function Check(Object)
    if Object.Name == "Crowbar" then
        return false
    end
    if Object:FindFirstChildOfClass("Part") or Object:FindFirstChildOfClass("MeshPart") then
        local Part = Object:FindFirstChildOfClass("Part") or Object:FindFirstChildOfClass("MeshPart")
        local Distance = (HumanoidRoot.Position - Part.Position).magnitude
        if Distance < Radius then
            return false
        end
        return true, Part
    end
    return false
end

local function InvetoryCheck(OldCFrame)
    local StorageAmount = Storage.UpperLine.Storage
    if StorageAmount.Text == "16/16 items" or StorageAmount.Text == "17/16 items" then
        HumanoidRoot.CFrame = OldCFrame
        wait()
        for i,v in pairs(Backpack:GetChildren()) do
            local A_1 = "Inventory_DropAll"
            local A_2 = {
                ["Tool"] = v.Name
            }
            Pickup:InvokeServer(A_1, A_2)
        end
        repeat task.wait() until StorageAmount.Text == "0/16 items"
    end
    return "Done"
end

local function AutoCollectFood()
    local OldCFrame = HumanoidRoot.CFrame
    local OldPosition = HumanoidRoot.Position
    local Amount = 0
    
    local FoodItems = {"Apple", "Banana", "Bloxy Soda", "Burger", "Cookie", "Dr. Bob Soda", "Hotdog", "Ice Cream", "Lemon", "Lemon Slice", "Medkit", "Pizza", "Water", "2 Litre Dr. Bob"}
    
    for i,v in pairs(WS:GetDescendants()) do
        if table.find(FoodItems, v.Name) then
            if v:FindFirstChild(v.Name) or v:FindFirstChild("Root") then
                local Bool, Part = Check(v)
                if Bool then
                    Amount = Amount + 1
                    if Amount >= TimesToTeleport then
                        HumanoidRoot.CFrame = OldCFrame
                        WindUI:Notify({
                            Title = "Auto Collect",
                            Content = "Collected " .. Amount .. " items!",
                            Duration = 3,
                        })
                        return
                    else
                        repeat task.wait()
                            repeat task.wait() until InvetoryCheck(OldCFrame) == "Done"
                            Bool, Part = Check(v)
                            if Part ~= nil then
                                HumanoidRoot.CFrame = Part.CFrame
                                local A_1 = "Store"
                                local A_2 = {
                                    ["Model"] = v
                                }
                                Pickup:InvokeServer(A_1, A_2)
                            end
                        until Part == nil
                    end
                end
            end
        end
    end
    
    HumanoidRoot.CFrame = OldCFrame
    WindUI:Notify({
        Title = "Auto Collect",
        Content = "Finished collecting " .. Amount .. " items!",
        Duration = 3,
    })
end

local function godmode(char)
    local hum = char:WaitForChild("Humanoid")
    hum.MaxHealth = math.huge
    hum.Health = math.huge
    hum.HealthChanged:Connect(function()
        hum.Health = hum.MaxHealth
    end)
end

local Window = WindUI:CreateWindow({
    Title = "CheatX - 3008",
    Icon = "home",
    Author = "Script Hub",
    HideSearchBar = false,
    Transparency = 0.4
})

local HomeTab = Window:Tab({
    Title = "Home",
})

HomeTab:Section({
    Title = "Camera",
})

HomeTab:Button({
    Title = "Unlock Third Person",
    Callback = function()
        Player.CameraMaxZoomDistance = 50
        Player.CameraMode = Enum.CameraMode.Classic
        WindUI:Notify({
            Title = "CheatX",
            Content = "Third person unlocked",
            Duration = 3,
        })
    end
})

HomeTab:Section({
    Title = "ESP Settings",
})

HomeTab:Toggle({
    Title = "ESP Players",
    Callback = function(Value)
        ESPSettings.Players = Value
        refreshESP()
    end
})

HomeTab:Toggle({
    Title = "ESP Employees",
    Callback = function(Value)
        ESPSettings.Employees = Value
        refreshESP()
    end
})

HomeTab:Toggle({
    Title = "ESP Food",
    Callback = function(Value)
        ESPSettings.Food = Value
        refreshESP()
    end
})

local PlayerTab = Window:Tab({
    Title = "Player",
})

PlayerTab:Section({
    Title = "WalkSpeed",
})

local speedValue = 16
local speedEnabled = false

PlayerTab:Toggle({
    Title = "Enable WalkSpeed",
    Callback = function(state)
        speedEnabled = state
        pcall(function()
            if state then
                Player.Character.Humanoid.WalkSpeed = speedValue
            else
                Player.Character.Humanoid.WalkSpeed = 16
            end
        end)
    end
})

PlayerTab:Slider({
    Title = "WalkSpeed",
    Value = { Min = 16, Max = 500, Default = 16 },
    Step = 1,
    Callback = function(v)
        speedValue = v
        if speedEnabled then
            pcall(function()
                Player.Character.Humanoid.WalkSpeed = v
            end)
        end
    end
})

PlayerTab:Section({
    Title = "JumpPower",
})

local jumpValue = 50
local jumpEnabled = false

PlayerTab:Toggle({
    Title = "Enable JumpPower",
    Callback = function(state)
        jumpEnabled = state
        pcall(function()
            if state then
                Player.Character.Humanoid.JumpPower = jumpValue
            else
                Player.Character.Humanoid.JumpPower = 50
            end
        end)
    end
})

PlayerTab:Slider({
    Title = "JumpPower",
    Value = { Min = 50, Max = 500, Default = 50 },
    Step = 1,
    Callback = function(v)
        jumpValue = v
        if jumpEnabled then
            pcall(function()
                Player.Character.Humanoid.JumpPower = v
            end)
        end
    end
})

PlayerTab:Section({
    Title = "Abilities",
})

PlayerTab:Button({
    Title = "Godmode",
    Callback = function()
        if Player.Character then
            godmode(Player.Character)
        end
        Player.CharacterAdded:Connect(godmode)
        
        WindUI:Notify({
            Title = "Godmode",
            Content = "Godmode activated!",
            Duration = 3,
        })
    end
})

local NoclipEnabled = false
local NoclipConnection = nil

PlayerTab:Toggle({
    Title = "Noclip",
    Callback = function(state)
        NoclipEnabled = state
        
        if state then
            NoclipConnection = RunService.Stepped:Connect(function()
                if NoclipEnabled and Player.Character then
                    for _, v in pairs(Player.Character:GetDescendants()) do
                        if v:IsA("BasePart") then
                            v.CanCollide = false
                        end
                    end
                end
            end)
        else
            if NoclipConnection then
                NoclipConnection:Disconnect()
                NoclipConnection = nil
            end
            
            if Player.Character then
                for _, v in pairs(Player.Character:GetDescendants()) do
                    if v:IsA("BasePart") then
                        v.CanCollide = true
                    end
                end
            end
        end
    end
})

local FarmTab = Window:Tab({
    Title = "Auto Farm",
})

FarmTab:Section({
    Title = "Auto Collect Settings",
})

FarmTab:Slider({
    Title = "Items to Collect",
    Value = { Min = 1, Max = 100, Default = 50 },
    Step = 1,
    Callback = function(v)
        TimesToTeleport = v
    end
})

FarmTab:Slider({
    Title = "Collection Radius",
    Value = { Min = 50, Max = 200, Default = 100 },
    Step = 10,
    Callback = function(v)
        Radius = v
    end
})

FarmTab:Space()

FarmTab:Button({
    Title = "Start Auto Collect Food",
    Callback = function()
        WindUI:Notify({
            Title = "Auto Collect",
            Content = "Starting food collection...",
            Duration = 3,
        })
        task.spawn(function()
            AutoCollectFood()
        end)
    end
})

local TeleportsTab = Window:Tab({
    Title = "Teleports",
})

TeleportsTab:Section({
    Title = "Item Teleports",
})

local itemsList = getUniqueItems()

TeleportsTab:Dropdown({
    Title = "Teleport to Item",
    Values = itemsList,
    Callback = function(selectedItem)
        teleportToItem(selectedItem)
    end
})

TeleportsTab:Space()

TeleportsTab:Button({
    Title = "Refresh Items List",
    Callback = function()
        itemsList = getUniqueItems()
        WindUI:Notify({
            Title = "Teleports",
            Content = "Items list refreshed! Found " .. #itemsList .. " items",
            Duration = 3,
        })
    end
})

local MiscTab = Window:Tab({
    Title = "Misc",
})

MiscTab:Section({
    Title = "Server",
})

MiscTab:Button({
    Title = "Rejoin Server",
    Callback = function()
        TeleportService:Teleport(game.PlaceId, Player)
    end
})

MiscTab:Button({
    Title = "Server Hop",
    Callback = function()
        local PlaceId = game.PlaceId
        local servers = HttpService:JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/" .. PlaceId .. "/servers/Public?sortOrder=Asc&limit=100"))
        
        if servers and servers.data then
            for _, server in pairs(servers.data) do
                if server.id ~= game.JobId then
                    TeleportService:TeleportToPlaceInstance(PlaceId, server.id, Player)
                    break
                end
            end
        end
    end
})

MiscTab:Space()

MiscTab:Toggle({
    Title = "Anti-AFK",
    Value = false,
    Callback = function(state)
        if state then
            Player.Idled:Connect(function()
                VirtualUser:CaptureController()
                VirtualUser:ClickButton2(Vector2.new())
            end)
        end
    end
})

MiscTab:Section({
    Title = "Settings",
})

MiscTab:Input({
    Title = "Change Keybind",
    Placeholder = "Enter new keybind",
    Callback = function(text)
        if text and #text > 0 then
            if isMobile then
                WindUI:Notify({
                    Title = "Mobile Device",
                    Content = "Cannot change keybind on mobile",
                    Duration = 2,
                })
                return
            end
            
            if #text > 1 then
                WindUI:Notify({
                    Title = "Invalid Keybind",
                    Content = "Keybind must be a single letter",
                    Duration = 2,
                })
                return
            end
            
            local keyName = string.upper(text)
            
            if keyName == "K" then
                WindUI:Notify({
                    Title = "Invalid Keybind",
                    Content = "This is already the default keybind",
                    Duration = 2,
                })
                return
            end
            
            if tonumber(keyName) then
                WindUI:Notify({
                    Title = "Invalid Keybind",
                    Content = "Cannot use numbers as keybind",
                    Duration = 2,
                })
                return
            end
            
            local restrictedKeys = {"A", "S", "D", "W", "SPACE", "UP", "DOWN", "LEFT", "RIGHT", "ESCAPE", "LEFTSHIFT", "SHIFT", "LSHIFT"}
            if table.find(restrictedKeys, keyName) then
                WindUI:Notify({
                    Title = "Invalid Key",
                    Content = "Cannot use movement keys",
                    Duration = 2,
                })
                return
            end
            
            local keyCode = Enum.KeyCode[keyName]
            if keyCode then
                Window:SetToggleKey(keyCode)
                
                Window:EditOpenButton({
                    Title = "Keybind: " .. keyName,
                    Icon = "grip",
                    CornerRadius = UDim.new(0,16),
                    StrokeThickness = 2,
                    OnlyMobile = false,
                    Enabled = true,
                    Draggable = false,
                })
                
                WindUI:Notify({
                    Title = "Keybind Updated",
                    Content = "Menu keybind changed to: " .. keyName,
                    Duration = 2,
                })
            else
                WindUI:Notify({
                    Title = "Invalid Keybind",
                    Content = "Keybind '" .. text .. "' is not valid",
                    Duration = 2,
                })
            end
        end
    end
})

Player.CharacterAdded:Connect(function(char)
    task.wait(0.5)
    local hum = char:FindFirstChildOfClass("Humanoid")
    if hum then
        if speedEnabled then
            hum.WalkSpeed = speedValue
        else
            hum.WalkSpeed = 16
        end
        
        if jumpEnabled then
            hum.JumpPower = jumpValue
        else
            hum.JumpPower = 50
        end
    end
    
    if NoclipEnabled then
        NoclipEnabled = false
        task.wait(0.1)
        NoclipEnabled = true
    end
end)

Player.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new())
end)

if not isMobile then
    Window:EditOpenButton({
        Title = "Keybind: K",
        Icon = "grip",
        CornerRadius = UDim.new(0,16),
        StrokeThickness = 2,
        OnlyMobile = false,
        Enabled = true,
        Draggable = false,
    })
    
    Window:SetToggleKey(Enum.KeyCode.K)
else
    Window:EditOpenButton({
        Title = "CheatX - 3008",
        Icon = "grip",
        CornerRadius = UDim.new(0,16),
        StrokeThickness = 2,
        OnlyMobile = true,
        Enabled = true,
        Draggable = true,
    })
end
